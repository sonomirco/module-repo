name: Test

on:
  workflow_dispatch:
  
jobs:
  # JOB to run change detection
  changes:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: GetValue
      run: |
      var=yq 'Gh.Module' versions.yaml
      echo $var
